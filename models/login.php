<?php
	class Login 
	{
		
		/*
		public $userID;
		public $password;

		public function __construct($userID, $password) 
		{
			$this->user_id   = $userID;
			$this->password  = $password;
		}

		public static function find($email) 
		{
			$db = Db::getInstance();
			$id = intval($id);
			$req = $db->prepare('SELECT userID, password FROM tbl_users WHERE email = :email');
			$req->execute(array('email' => $email));
			$count = $req->rowCount();
			$row = $req->fetch();

			return new Login($row['userID'], $row['password']);
		}
		
		*/
	}
?>